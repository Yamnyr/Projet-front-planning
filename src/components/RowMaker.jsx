import React from "react";
import PropTypes from 'prop-types';

function RowMaker({}) {
    return (
        <div>
        </div>
    );
}

RowMaker.propTypes = {
};

RowMaker.defaultProps = {
};

export default RowMaker;